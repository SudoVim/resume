import { selectors as skills } from "./skills";
import { selectors as experience } from "./experience/selectors";
import { selectors as mastermind } from "./mastermind/selectors";

export const selectors = {
  skills,
  experience,
  mastermind,
};
